from game.actor import Actor

class Brick(Actor):
    """
    """
    def __init__(self, x, y):
        """
        """
        position = Point(x, y)
        brick.set_text("*")
        brick.set_position(position)